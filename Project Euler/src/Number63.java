public class Number63
{
	public static void main(String[] args)
	{
		long number = 1;
		int counter = 0;
		int z = 1;
		long length = 9999999999999L;
		while (number <= length)
		{
			String power = Integer.toString((int) number);
			int x = power.length();
			z = 1;
			if (x == 1 && z == 1)
			{
				int number1 = 0;
				int expo = 1;
				int prod = (int) Math.pow(number1,expo);
				while (prod <= number)
				{
					number1++;
					prod = (int) Math.pow(number1,expo);
					if (prod == number)
					{
						counter++;
						System.out.println(number + " = " + number1 + "^" + expo + "; counter is at " +  counter);
						number1 = 1;
						z = 0;
						break;
					}
				}
				number++;
				z = 0;
			}
			if (x == 2 && z == 1)
			{
				int number1 = 1;
				int expo = 2;
				int prod = (int) Math.pow(number1,expo);
				while (prod <= number)
				{
					number1++;
					prod = (int) Math.pow(number1,expo);
					if (prod == number)
					{
						counter++;
						System.out.println(number + " = " + number1 + "^" + expo + "; counter is at " +  counter);
						number1 = 1;
						z = 0;
						break;
					}
				}
				number++;
				z = 0;
			}
			if (x == 3 && z == 1)
			{
				int number1 = 1;
				int expo = 3;
				int prod = (int) Math.pow(number1,expo);
				while (prod <= number)
				{
					number1++;
					prod = (int) Math.pow(number1,expo);
					if (prod == number)
					{
						counter++;
						z = 0;
						System.out.println(number + " = " + number1 + "^" + expo + "; counter is at " +  counter);
						number1 = 1;
						break;
					}
				}
				number++;
				z = 0;
			}
			if (x == 4 && z == 1)
			{
				int number1 = 1;
				int expo = 4;
				int prod = (int) Math.pow(number1,expo);
				while (prod <= number)
				{
					number1++;
					prod = (int) Math.pow(number1,expo);
					if (prod == number)
					{
						counter++;
						System.out.println(number + " = " + number1 + "^" + expo + "; counter is at " +  counter);
						number1 = 1;
						z = 0;
						break;
					}
				}
				number++;
				z = 0;
			}
			if (x == 5 && z == 1)
			{
				int number1 = 1;
				int expo = 5;
				int prod = (int) Math.pow(number1,expo);
				while (prod <= number)
				{
					number1++;
					prod = (int) Math.pow(number1,expo);
					if (prod == number)
					{
						counter++;
						System.out.println(number + " = " + number1 + "^" + expo + "; counter is at " +  counter);
						number1 = 1;
						z = 0;
						break;
					}
				}
				number++;
				z = 0;
			}
			if (x == 6 && z == 1)
			{
				int number1 = 1;
				int expo = 6;
				int prod = (int) Math.pow(number1,expo);
				while (prod <= number)
				{
					number1++;
					prod = (int) Math.pow(number1,expo);
					if (prod == number)
					{
						counter++;
						System.out.println(number + " = " + number1 + "^" + expo + "; counter is at " +  counter);
						number1 = 1;
						z = 0;
						break;
					}
				}
				number++;
				z = 0;
			}
			if (x == 7 && z == 1)
			{
				int number1 = 1;
				int expo = 7;
				int prod = (int) Math.pow(number1,expo);
				while (prod <= number)
				{
					number1++;
					prod = (int) Math.pow(number1,expo);
					if (prod == number)
					{
						counter++;
						System.out.println(number + " = " + number1 + "^" + expo + "; counter is at " +  counter);
						number1 = 1;
						z = 0;
						break;
					}
				}
				number++;
				z = 0;
			}
			if (x == 8 && z == 1)
			{
				int number1 = 1;
				int expo = 8;
				int prod = (int) Math.pow(number1,expo);
				while (prod <= number)
				{
					number1++;
					prod = (int) Math.pow(number1,expo);
					if (prod == number)
					{
						counter++;
						System.out.println(number + " = " + number1 + "^" + expo + "; counter is at " +  counter);
						number1 = 1;
						z = 0;
						break;
					}
				}
				number++;
				z = 0;
			}
			if (x == 9 && z == 1)
			{
				int number1 = 1;
				int expo = 9;
				int prod = (int) Math.pow(number1,expo);
				while (prod <= number)
				{
					number1++;
					prod = (int) Math.pow(number1,expo);
					if (prod == number)
					{
						counter++;
						System.out.println(number + " = " + number1 + "^" + expo + "; counter is at " +  counter);
						number1 = 1;
						z = 0;
						break;
					}
				}
				number++;
				z = 0;
			}
			if (x == 10 && z == 1)
			{
				int number1 = 1;
				int expo = 10;
				int prod = (int) Math.pow(number1,expo);
				while (prod <= number)
				{
					number1++;
					prod = (int) Math.pow(number1,expo);
					if (prod == number)
					{
						counter++;
						System.out.println(number + " = " + number1 + "^" + expo + "; counter is at " +  counter);
						number1 = 1;
						z = 0;
						break;
					}
				}
				number++;
				z = 0;
			}
			if (x == 11 && z == 1)
			{
				int number1 = 1;
				int expo = 11;
				int prod = (int) Math.pow(number1,expo);
				while (prod <= number)
				{
					number1++;
					prod = (int) Math.pow(number1,expo);
					if (prod == number)
					{
						counter++;
						System.out.println(number + " = " + number1 + "^" + expo + "; counter is at " +  counter);
						number1 = 1;
						z = 0;
						break;
					}
				}
				number++;
				z = 0;
			}
			if (x == 12 && z == 1)
			{
				int number1 = 1;
				int expo = 12;
				int prod = (int) Math.pow(number1,expo);
				while (prod <= number)
				{
					number1++;
					prod = (int) Math.pow(number1,expo);
					if (prod == number)
					{
						counter++;
						System.out.println(number + " = " + number1 + "^" + expo + "; counter is at " +  counter);
						number1 = 1;
						z = 0;
						break;
					}
				}
				number++;
				z = 0;
			}
			if (x == 13 && z == 1)
			{
				int number1 = 1;
				int expo = 13;
				int prod = (int) Math.pow(number1,expo);
				while (prod <= number)
				{
					number1++;
					prod = (int) Math.pow(number1,expo);
					if (prod == number)
					{
						counter++;
						System.out.println(number + " = " + number1 + "^" + expo + "; counter is at " +  counter);
						number1 = 1;
						z = 0;
						break;
					}
				}
				number++;
				z = 0;
			}
		}
		System.out.println("The answer to Project Euler #63 is " + counter);
	}

}
